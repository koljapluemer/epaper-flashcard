Web (web!!!) research if you're unsure about how to interact with the dev board.
Isolate tests and debugging in their own files so they can be easily stripped from final builds.
If ESP is plugged in, actually flash code changes.